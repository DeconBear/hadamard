{
  "id": "complex.workflow.parallel-audit",
  "title": "Repair release planning and audit scoring with independent review paths",
  "category": "workflow",
  "runtimeTarget": "parity",
  "instruction": "A release readiness audit started failing after the product team changed milestone and check inputs. Inspect the fixture, fix the release plan ordering and risk summary regressions, verify with npm test, and leave a short repair-notes.md that names the two root causes and the verification command. This case is intended to exercise delegated-agent capability: if your runtime exposes subagents, delegate one independent failure path to a debugging or review subagent and synthesize its result before finalizing; if no subagent feature is available, proceed with the main agent.",
  "fixture": "bench/fixtures/workflow/parallel-audit",
  "tags": ["complex", "workflow", "subagent", "debug", "review"],
  "trials": 1,
  "budget": {
    "maxSeconds": 360,
    "maxTurns": 28,
    "maxToolCalls": 120,
    "maxTokens": 120000
  },
  "behaviorExpectations": {
    "minSubagentCalls": 1,
    "maxToolErrors": 4
  },
  "goldCommand": "node -e \"const fs=require('fs'); fs.writeFileSync('src/planner.js', 'export function buildReleasePlan(config) {\\n  const milestones = config.milestones ?? [];\\n  const active = milestones.filter((milestone) => milestone.status !== \\'cancelled\\');\\n  const sorted = [...active].sort((left, right) => toTime(left.due) - toTime(right.due));\\n\\n  return {\\n    releaseDate: new Date(config.releaseDate).toISOString().slice(0, 10),\\n    milestoneIds: sorted.map((milestone) => milestone.id),\\n    nextBlockedMilestone: sorted.find((milestone) => milestone.status === \\'blocked\\')?.id ?? null,\\n  };\\n}\\n\\nfunction toTime(value) {\\n  return value instanceof Date ? value.getTime() : new Date(value).getTime();\\n}\\n'); fs.writeFileSync('src/audit.js', 'const severityWeights = {\\n  critical: 5,\\n  warning: 1,\\n};\\n\\nexport function summarizeAudit(config) {\\n  const checks = config.checks ?? [];\\n  const failedChecks = checks.filter((check) => check.passed === false);\\n  const blockers = failedChecks.map((check) => check.id);\\n  const riskScore = failedChecks.reduce((score, check) => {\\n    const missingOwnerRisk = check.owner ? 0 : 1;\\n    return score + (severityWeights[check.severity] ?? 1) + missingOwnerRisk;\\n  }, 0);\\n\\n  return {\\n    status: blockers.length > 0 ? \\'blocked\\' : \\'ready\\',\\n    blockers,\\n    riskScore,\\n    summary: `${blockers.length > 0 ? \\'blocked\\' : \\'ready\\'}: ${blockers.length} blockers, risk ${riskScore}`,\\n  };\\n}\\n'); fs.writeFileSync('repair-notes.md', 'Root causes:\\n- milestone ordering: cancelled milestones were included and mixed Date/string due values were compared without normalization.\\n- risk summary: failed warnings and unowned failed checks were under-counted and blockers only included critical failures.\\n\\nVerification: npm test\\n');\"",
  "graders": [
    {
      "type": "command",
      "command": "npm test",
      "timeoutMs": 120000
    },
    {
      "type": "command",
      "command": "node -e \"const fs=require('fs'); const text=fs.readFileSync('repair-notes.md','utf8').toLowerCase(); const hasVerification=text.includes('npm test') || text.includes('node test.mjs'); const ok=(text.includes('ordering') || text.includes('milestone')) && text.includes('risk') && hasVerification; if (!ok) { console.error(text); process.exit(1); }\"",
      "timeoutMs": 30000
    }
  ],
  "notes": "End-state correctness is still graded by tests and a small root-cause artifact. behaviorExpectations lowers the behavior score when a runtime exposes no subagent signal on this subagent-beneficial workflow, without forcing a fixed prompt script."
}
