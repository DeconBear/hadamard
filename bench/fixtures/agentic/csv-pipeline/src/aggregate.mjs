// groupBy(rows, key): a Map from each distinct row[key] to the array of rows
// that have it, in original order.
export function groupBy(rows, key) {
  throw new Error('not implemented: groupBy');
}

// sumBy(rows, key, field): an object mapping each distinct row[key] to the sum
// of Number(row[field]) across the rows in that group.
export function sumBy(rows, key, field) {
  throw new Error('not implemented: sumBy');
}
