// Parse CSV text into an array of row objects keyed by the header (first) row.
// Must support quoted fields that contain commas and escaped double-quotes ("" -> ").
// Empty input, or input with only a header row, yields an empty array.
export function parseCsv(text) {
  throw new Error('not implemented: parseCsv');
}
