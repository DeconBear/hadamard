// toTable(rows): render rows as a text table. Columns are the union of keys in
// first-appearance order. Line 0 is the header (column names); each subsequent
// line is one row. Cells in a line are separated by " | " and padded with spaces
// so each column is as wide as its widest cell. There is NO separator line
// between the header and the rows. See test/pipeline.test.mjs for the exact shape.
export function toTable(rows) {
  throw new Error('not implemented: toTable');
}
